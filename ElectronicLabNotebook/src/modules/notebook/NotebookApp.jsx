/**
 * NotebookApp.jsx
 * The Notebook window is entirely WhitePages.
 * AI Autocomplete, Text → Table, and Text → Image are embedded
 * directly inside WhitePages (sidebar tools + background autocomplete).
 */

import WhitePages from './WhitePages';

const NotebookApp = () => <WhitePages />;

export default NotebookApp;